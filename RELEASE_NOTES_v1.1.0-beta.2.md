# TG工具箱 v1.1.0-beta.2

> 测试版（beta）。遇到问题请把程序 `logs` 目录最新日志发给开发者。

## 更新内容（相对 beta.1）

### 修复
- **刷新账号资料崩溃**：Telegram 服务器启用新版 `user` 构造体（`b1b8cc83`），Telethon 1.44 不识别导致报 `Could not find a matching Constructor ID`。已按官方 schema 注册补丁构造体并防御未知尾部字段
- **资料编辑名字/姓氏抓取错误**：此前「名字」栏会被填入「姓+名」拼接串，保存还会丢姓氏；现在取真实字段
- **顶栏按钮折行**：加 nowrap，窗口窄时不再挤成两行

### 界面
- 顶栏还原纯文本标题；版本号移至「设置 → 关于」（含版本号与仓库地址）
- 个人简介输入框：换行自动加高（field-sizing + JS 双保险）
- 「加群频道」设置里的链接条目改两行布局（类型+备注+删除 / 链接整行），修复溢出弹窗

## 附件

- `TG-tools_v1.1.0-beta.2.zip`（约 28 MB）：解压后双击 `TG工具箱Web.exe`；首次启动 SmartScreen 拦截时选「更多信息 → 仍要运行」
