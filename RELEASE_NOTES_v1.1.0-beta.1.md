# TG工具箱 v1.1.0-beta.1

> 测试版（beta）。遇到问题请把程序 `logs` 目录最新日志发给开发者。

## 更新内容

### 新功能
- **多账号同时在线**：连接池架构，双击在线账号秒切（不重连）；右键菜单可单独断开某个账号（其他保持在线）
- **聊天**：会话列表 / 历史消息查看；底部输入框可发送文本消息（回车发送）
- **加群频道**：右栏控制页——群 / 频道分板块、备注名显示、单个加入或全部加入；链接条目在设置里维护
- **tdata 账号直连**：仅 tdata 的账号双击即连（自动转换 session，无需手动点转换）
- **启动客户端**：顶栏一键启动当前账号目录内的 Telegram 便携版
- **文件夹重命名**：右键菜单打开弹窗改名，自动同步分组/资料/头像缓存
- **设置**：日志等级（debug 含蓝牙原始诊断）；白名单管理移入设置弹窗

### 修复
- passkey：修复二维码 CBOR 编码（字段5须为文本字符串）与 Noise 握手两处协议 bug（与 tdesktop 逐字节对齐）
- 头像刷新：在线账号复用连接池 client（避免 session 锁）；修复 avatars 路径被历史文件占用导致全部下载失败
- 头像显示防 WebView2 缓存（URL 加版本参数 + no-cache）
- 卡片折叠记忆重启不生效；中栏卡片全开时穿模（改滚动）
- 刷新失败原因透明化：所有失败分支打具体日志到界面

### 界面
- 新建分组 / 文件夹重命名 M3 弹窗；移动分组 M3 勾选框
- 右栏「返回」改为「日志」并支持记忆返回；消息列表刷新按钮移位
- 重连时顶栏显示「重新连接…」；「一键获取头像」改为「刷新账号信息」（仅当前账号）
- 顶栏显示程序版本号

## 版本号规则

`X.Y.Z`（大版本.小版本.微调），测试版带 `-beta.N` 后缀。

## 校验

- 文件：`TG工具箱Web_v1.1.0-beta.1.zip`（约 28 MB）
- 解压后双击 `TG工具箱Web.exe`；首次启动 SmartScreen 拦截时选「更多信息 → 仍要运行」
