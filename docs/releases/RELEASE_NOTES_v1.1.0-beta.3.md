# TG工具箱 v1.1.0-beta.3

> 测试版（beta）。遇到问题请把程序 `logs` 目录最新日志发给开发者。

## 更新内容（相对 beta.2）

### 新功能
- **账号轮询**：「其他设置 → 账号轮询」一键对全部账号逐个登录保活 + 测活（每号间隔 1s 防风控，温和可停）。结果落盘 `poll_results.json`，**死号在账号列表红色标记**，悬停看原因与检测时间
- **删除账号**：「其他设置 → 删除账号」，二级确认弹窗醒目显示将被删除的文件夹完整路径，勾选确认后执行；后端多重防护（仅限根下一级账号文件夹、程序目录拒绝删除、在线自动断开），删后自动清理分组/资料/头像/轮询缓存
- **分组管理**：分组标签直接**拖动排序**（持久化）；**右键分组**可重命名 / 删除（删除分组时组内账号回到「未分组」，M3 弹窗确认）

### 测活准确性
- 只有**封禁/注销**才判定死号；session 失效（可重新登录恢复）与网络失败一律不判死
- 连接失败自动重试 3 次；轮询连接 30 秒超时

## 附件

- `TG-tools_v1.1.0-beta.3.zip`（约 29 MB）：解压后双击 `TG工具箱Web.exe`；首次启动 SmartScreen 拦截时选「更多信息 → 仍要运行」
