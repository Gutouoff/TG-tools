# TG工具箱 v1.1.0-beta.7

> 测试版（beta）。遇到问题请把程序 `logs` 目录最新日志发给开发者。

## 更新内容（相对 beta.6）

### 新功能
- **「转换」功能组**（中栏新卡片）：
  - **tdata 转 s+s**：用 tdata 覆盖刷新 session+json，专修「session 过期导致测活失败」的账号（在线账号只刷 json 元数据）
  - **s+s 转 tdata**：生成 Telegram Desktop 登录态（目标 tdata 非空时拒绝执行）
  - **刷新 s+s 数据**：批量自动修复所有「有 tdata 且测活失败」的账号（每号间隔 1s，可停止）
- **拖包导入增强**：支持仅含 session 的压缩包（凭据 json 首次连接自动补建）；导入后自动放置 Telegram.exe，「启动客户端」开箱即用
- **账号轮询限定分组**：左侧选中哪个分组（或「未分组」），轮询就只跑该范围的账号

### 修复 / 体验
- 轮询完成后死号标识自动随最新结果刷新
- 日志自动滚动到最底部
- 连接缺凭据 json 的账号自动补建（自愈），不再报 `lost sys.stdin`

## 附件

- `TG-tools_v1.1.0-beta.7.zip`（约 29 MB）：解压后双击 `TG工具箱Web.exe`；首次启动 SmartScreen 拦截时选「更多信息 → 仍要运行」
- 此前修复继续有效：启动白屏自动降浏览器模式（beta.4）、tdata 转换缺文件（beta.5）、channel 构造体补丁（beta.6）
