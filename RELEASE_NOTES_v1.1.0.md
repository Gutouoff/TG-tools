# TG工具箱 v1.1.0 正式版

> 相对 1.0.x 的完整更新（beta.1 ~ beta.9 测试累计），感谢各位测试者的反馈。

## 多账号

- **多账号同时在线**（连接池）：双击在线账号秒切，无需重连；右键可单独断开
- **分组管理**：拖动排序（持久化）、右键重命名/删除（成员回未分组）
- **账号轮询**：逐号登录保活 + 测活（每号间隔 1s 防风控），可限定当前分组；死号红色标记、悬停看原因；三态判定——仅封禁/注销判死号，session 失效与网络失败不误判
- **格式转换**（新功能组）：tdata ↔ session+json 双向转换（2FA 账号支持手填密码）、批量刷新过期 session、覆盖选项（默认关闭）
- **拖包导入**：拖入 zip 自动建账号，支持仅 session 的包；自动放置 Telegram.exe
- **删除账号**（二级确认 + 显示路径）、**文件夹重命名**（快捷填充：姓名/username/手机号/用户ID，设置可配置 + 自定义前缀）
- **启动客户端**：一键启动账号的 Telegram 便携版；右键打开账号文件夹
- **安装升级客户端**：更新 Telegram 便携版，并给缺客户端的账号文件夹自动补装

## 聊天

- 会话列表 / 历史消息 / 发送文本消息
- 输入用户名直接发起聊天
- 消息接收开关与过滤规则（排除频道/群组/机器人）

## 安全

- 通行密钥（passkey）注册 / 删除（蓝牙 caBLE 扫码，二维码引导）
- 两步验证（2FA）设置、登录邮箱验证、登录设备管理

## 工具

- 导出账号总表.xlsx（用户名/ID/手机号/邮箱/通行密钥/2FA/注册时间/状态）
- tdata → session 自动转换（拖包或连接时自动）
- 代理支持（system / manual / SOCKS5）

## 体验

- 浅色/深色主题、色彩自定义、卡片排序与折叠记忆、中栏窄屏自适应单列
- 搜索：姓名 / username / 手机号 / 用户ID（@ 前缀忽略、纯数字免输区号）
- 日志自动滚底、日志等级（debug 含蓝牙诊断）、白名单移入设置
- 窗口模式失败自动降级浏览器模式（缺 .NET / 文件锁定等环境问题不再报错）

## 稳定性修复

- Telegram 服务端新 TL 构造体（message / user / channel）自动补丁注册
- 全新环境 tdata 转换缺文件修复（opentele devices.json）
- 所有接口错误统一返回 JSON 中文原因，不再出现不可读的 500
- 头像缓存、会话、轮询、转换等多处问题修复

## 附件与运行

- `TG-tools_v1.1.0.zip`（约 30 MB）：解压后双击 `TG工具箱Web.exe`；首次启动 SmartScreen 拦截时选「更多信息 → 仍要运行」
- 账号根目录与程序目录同级；若窗口模式报错（缺 .NET），程序会自动切浏览器模式

## 许可证

[GPL-3.0](LICENSE)：可自由使用、修改、商用；衍生版本分发必须开源并沿用 GPL-3.0，禁止闭源。
